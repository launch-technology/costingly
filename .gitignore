# Secrets — never commit these
.env
.env.*
!.env.example
# `.env.*` above catches this too, and the .env.example exception does not
# cover it — without this line the template would be silently uncommittable.
!.env.sandbox.example

# Dependencies
node_modules/

# Build output
dist/
.next/
.vercel/

# Local Postgres data (docker-compose volume, if bind-mounted)
pgdata/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
.idea/
.vscode/
